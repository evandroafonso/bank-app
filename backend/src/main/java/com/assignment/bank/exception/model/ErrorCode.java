package com.assignment.bank.exception.model;

public enum ErrorCode {
    NOT_FOUND,
    VALIDATION_ERROR,
    BUSINESS_ERROR,
    FORBIDDEN,
    INTERNAL_ERROR,
    DUPLICATE_RESOURCE,
    INSUFFICIENT_BALANCE,
    CURRENCY_MISMATCH,
    TRANSACTION_DENIED
}