import { SmartCurrencyPipe } from './smart-currency-pipe';

describe('SmartCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
